services:
  frontend:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-https://www.uygunucretliterapi.com/api}
    container_name: psiko_frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
