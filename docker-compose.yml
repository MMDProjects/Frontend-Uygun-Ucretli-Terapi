services:
  frontend:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://213.159.30.37:4000}
    container_name: psiko_frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
